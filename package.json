{
  "private": true,
  "engines": {
    "node": "=16"
  },
  "scripts": {
    "lint": "pnpm -r run lint",
    "lint:fix": "pnpm -r run lint:fix",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "@suzulabo/eslint-config": "0.0.4-test.1",
    "@suzulabo/prettier-config": "^0.0.2",
    "@suzulabo/tsconfig": "^0.0.2",
    "typescript": "^4.9.4"
  }
}
